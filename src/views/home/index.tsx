import React from "react";

interface Props {}

const Home = (props: Props) => {
  return (
    <div className="relative h-full w-full flex">
      <h2 className="text-3xl">Todo App | React, Redux Saga</h2>
    </div>
  );
};

export default Home;
